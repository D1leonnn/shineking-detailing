{
  "name": "shineking-detailing",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.3.0",
    "@astrojs/node": "^10.0.6",
    "astro": "^6.2.1",
    "groq-sdk": "^1.1.2"
  },
  "devDependencies": {
    "wrangler": "^4.87.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
